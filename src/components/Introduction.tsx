const Introduction = () => {
  return (
    <div className="prose prose-invert max-w-3xl mx-auto animate-fade-in-up" style={{ animationDelay: '0.5s' }}>
      <p className="text-muted-foreground">
        My life moves in patterns, cycles, and quiet revelations.
        Every idea I craft is a reflection of my inner path —
        a rhythm written in numbers, intuition, and purpose.
      </p>
    </div>
  );
};

export default Introduction;
